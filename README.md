# Frontend test case - POS application

## Available Scripts
* `yarn start` - start application in development mode ([http://localhost:3000](http://localhost:3000))
* `yarn build` - build the project
* `yarn test` - run tests
* `yarn deploy` - deploy project to Github Pages